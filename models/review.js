class Review {
    constructor(id, userId, album, imageUrl, rating, text) {
        this.id = id,
        this.userId = userId,
        this.album = album,
        this.artist = artist,
        this.imageUrl = imageUrl,
        this.rating = rating,
        this.text = text
    }
}

export default Review